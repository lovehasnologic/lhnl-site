---
title: Writing
---

.section.section--padded
  %h1.section__title= current_page.data.title
  %h2.section__subtitle #{tagname} Articles
  %nav.listNav
    %ul.listNav__list
      - articles.each do |article|
        %li.listNav__item
          = link_to article.url, class: "listNav__link" do
            = article.title
            %time.listNav__secondary{ datetime: article.date.strftime('%F') }= article.date.strftime("%B '%y")
